<div class="Comic-drawer">
  {{#each comic in model itemController="comic"}}
    <div class="Comic">
      {{#md-card image=comic.thumbnailURL}}
        {{#md-card-content}}
          {{comic.title}}
        {{/md-card-content}}
      {{/md-card}}
    </div>
  {{/each}}
</div>
<div class="center-align">
  {{md-btn id="loadmore" text="Load more comics" buttonType='large'}}
</div>
